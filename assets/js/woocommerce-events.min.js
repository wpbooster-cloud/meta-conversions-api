 (function($) { 'use strict'; $(document).ready(function() { if (typeof fbq === 'undefined') { console.warn('Meta CAPI: Facebook Pixel (fbq) not loaded. WooCommerce events will not be tracked.'); return; } const MetaCAPIWooCommerce = { init: function() { this.trackViewContent(); this.trackAddToCart(); this.trackInitiateCheckout(); this.trackPurchase(); }, generateEventId:function(eventName,uniqueId=''){const eventType=eventName.toLowerCase().replace(/[^a-z0-9]/g,'');const timestamp=Date.now();if(uniqueId&&uniqueId.trim()!==''){return eventType+'_'+uniqueId+'_'+timestamp;}else{return eventType+'_'+timestamp;}}, trackViewContent: function() { if (typeof metaCAPIWooCommerceData === 'undefined' || !metaCAPIWooCommerceData.is_product) { return; } const productData = metaCAPIWooCommerceData.product; if (!productData) { return; } const eventId = this.generateEventId('ViewContent', productData.id); fbq('track', 'ViewContent', { content_ids: [productData.id], content_name: productData.name, content_type: 'product', content_category: productData.category || '', value: productData.price, currency: productData.currency }, { eventID: eventId }); console.log('Meta CAPI: ViewContent event tracked', { product_id: productData.id, event_id: eventId }); }, trackAddToCart: function() { const self = this; $(document.body).on('added_to_cart', function(event, fragments, cart_hash, button) { const productId = button.data('product_id'); const quantity = button.data('quantity') || 1; let productData = { id: productId, name: button.data('product_name') || button.attr('aria-label') || 'Product', price: parseFloat(button.data('product_price')) || 0, quantity: quantity }; if (typeof metaCAPIWooCommerceData !== 'undefined' && metaCAPIWooCommerceData.product) { productData = metaCAPIWooCommerceData.product; productData.quantity = quantity; } const eventId = self.generateEventId('AddToCart', productId); const currency = productData.currency || (typeof metaCAPIWooCommerceData !== 'undefined' ? metaCAPIWooCommerceData.currency : 'USD'); fbq('track', 'AddToCart', { content_ids: [productData.id], content_name: productData.name, content_type: 'product', value: productData.price * quantity, currency: currency }, { eventID: eventId }); console.log('Meta CAPI: AddToCart event tracked (AJAX)', { product_id: productId, quantity: quantity, event_id: eventId }); }); $('form.cart').on('submit', function() { if (typeof metaCAPIWooCommerceData === 'undefined' || !metaCAPIWooCommerceData.product) { return; } const productData = metaCAPIWooCommerceData.product; const quantityInput = $(this).find('input[name="quantity"]'); const quantity = quantityInput.length ? parseInt(quantityInput.val()) : 1; const eventId = self.generateEventId('AddToCart', productData.id); fbq('track', 'AddToCart', { content_ids: [productData.id], content_name: productData.name, content_type: 'product', value: productData.price * quantity, currency: productData.currency }, { eventID: eventId }); console.log('Meta CAPI: AddToCart event tracked (Form)', { product_id: productData.id, quantity: quantity, event_id: eventId }); }); }, trackInitiateCheckout: function() { if (typeof metaCAPIWooCommerceData === 'undefined' || !metaCAPIWooCommerceData.is_checkout) { return; } const cartData = metaCAPIWooCommerceData.cart; if (!cartData) { return; } const eventId = this.generateEventId('InitiateCheckout'); fbq('track', 'InitiateCheckout', { content_ids: cartData.content_ids || [], contents: cartData.contents || [], content_type: 'product', value: cartData.value || 0, currency: cartData.currency, num_items: cartData.num_items || 0 }, { eventID: eventId }); console.log('Meta CAPI: InitiateCheckout event tracked', { value: cartData.value, num_items: cartData.num_items, event_id: eventId }); }, trackPurchase: function() { if (typeof metaCAPIWooCommerceData === 'undefined') { console.warn('Meta CAPI: WooCommerce data not available for Purchase tracking'); return; } if (!metaCAPIWooCommerceData.is_order_received) { console.log('Meta CAPI: Not on order received page, skipping Purchase event'); return; } const purchaseTiming = metaCAPIWooCommerceData.purchase_timing || 'placed'; if (purchaseTiming === 'paid') { console.log('Meta CAPI: Purchase timing set to "payment confirmed", skipping browser-side event (server-only tracking)'); return; } const orderData = metaCAPIWooCommerceData.order; if (!orderData || !orderData.id) { console.warn('Meta CAPI: Order data not available', { has_order_data: !!orderData, wc_data: metaCAPIWooCommerceData }); return; } const eventId = 'purchase_' + orderData.id; const purchaseData = { content_ids: orderData.content_ids || [], contents: orderData.contents || [], content_type: 'product', value: parseFloat(orderData.value) || 0, currency: orderData.currency || metaCAPIWooCommerceData.currency || 'USD', num_items: parseInt(orderData.num_items) || 0 }; if (typeof fbq === 'undefined') { console.error('Meta CAPI: Facebook Pixel (fbq) not loaded. Purchase event not tracked.'); return; } fbq('track', 'Purchase', purchaseData, { eventID: eventId }); console.log('Meta CAPI: Purchase event tracked (browser-side)', { order_id: orderData.id, value: purchaseData.value, currency: purchaseData.currency, num_items: purchaseData.num_items, event_id: eventId }); } }; MetaCAPIWooCommerce.init(); }); })(jQuery); 